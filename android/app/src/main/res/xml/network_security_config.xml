<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Configuration pour servo.up.railway.app - Accepter HTTP et HTTPS -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">servo.up.railway.app</domain>
        <domain includeSubdomains="true">*.railway.app</domain>
        <!-- Accepter tous les certificats pour Railway (y compris auto-signés) -->
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
        <!-- Pas de pinning de certificat pour plus de flexibilité -->
    </domain-config>

    <!-- Configuration pour localhost/développement (HTTP autorisé) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>

    <!-- Configuration par défaut stricte (HTTPS uniquement, certificats vérifiés) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">.</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
